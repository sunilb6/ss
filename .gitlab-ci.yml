image: openjdk:17

before_script:
  - apt-get update

stages:
  - build
  - test

build:
  stage: build
  script: ./gradlew build
  artifacts:
    paths:
      - build/libs/securesave-*.jar

unitTests:
  stage: test
  script:
    - ./gradlew test